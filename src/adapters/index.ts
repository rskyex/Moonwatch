// ---------------------------------------------------------------------------
// Moonwatch – adapter registry
// ---------------------------------------------------------------------------
// Future adapter registry
// To add a new source: create a new adapter file, implement the Adapter
// interface, and register it here. The transform layer will normalize
// outputs into internal types.
// ---------------------------------------------------------------------------

export { type Adapter, type RawItem } from "./types";
